import React, { Component } from 'react';

class Trains extends Component {
    render() {
        return (
            <div>
                this is a where the train statuses
            </div>
        );
    }
}

export default Trains;